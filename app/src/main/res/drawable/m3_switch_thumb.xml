<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Outer transparent square (intrinsic 28x28) to guarantee 1:1 bounds -->
    <item>
        <shape android:shape="rectangle">
            <size android:width="28dp" android:height="28dp" />
            <solid android:color="@android:color/transparent" />
        </shape>
    </item>

    <!-- Centered circular knob (20dp) via equal insets. Add a 1dp outline
         so the knob edge renders crisply as a perfect circle on any track. -->
    <item android:left="4dp" android:top="4dp" android:right="4dp" android:bottom="4dp">
        <shape android:shape="oval">
            <size android:width="20dp" android:height="20dp" />
            <solid android:color="@color/pure_white" />
            <stroke android:width="1dp" android:color="?attr/colorOutline" />
        </shape>
    </item>
</layer-list>
